# Android-Fragments
Retaining data across configuration changes. Implemented broadcast receivers to receive broadcast from other app(A1). App A1 can send a broadcast without the permission granted by the user. 
